/** @type {import('next').NextConfig} */
const nextConfig = {};

// Triggering a server restart to resolve module caching issues and refresh the preview.
// This comment was added to force a rebuild.

export default nextConfig;
